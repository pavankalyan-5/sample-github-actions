name: Hello World Workflow

on:
  push:
    branches:
      - main
      - production


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run a script
        run: echo "Hello, world!"

      - name: Print current Date
        run: date